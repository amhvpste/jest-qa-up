import { addByX } from '../addByX';

describe('addByX', () => {
  it.todo('should return a function');

  it.todo(
    'should return a function which takes one argument and returns a sum with a value from original function'
  );
});
